version: "2"
services:
    www:
        build: .
        ports: 
            - "8086:80"
        volumes:
            - ./www:/var/www/html/
        networks:
            - default
volumes:
    persistent: